# Upstream v1.18.2 StartOS Sync Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Merge upstream Hashrate Autopilot v1.18.2 onto the current Start9 Community intake branch and produce a fully inspected `1.18.2:0` multi-architecture StartOS candidate.

**Architecture:** Preserve the repository as a history-retaining application fork: merge the upstream release tag, then restore only the documented StartOS-owned divergences. Keep application migrations inside upstream and update the StartOS version graph in place because this wrapper adds no package-level data migration.

**Tech Stack:** Git, Node.js 22, npm workspaces, TypeScript, Vitest, StartOS SDK 2, `start-cli`, Docker Buildx, GNU Make.

---

### Task 1: Merge the Upstream Release

**Files:**
- Modify: upstream-owned files changed by tag `v1.18.2`
- Resolve: only merge conflicts reported by Git

**Step 1: Confirm the exact base and tag**

Run: `git rev-parse HEAD && git rev-parse 'v1.18.2^{}' && git status --short`

Expected: HEAD descends from Start9 branch `chore/upstream-v1.18.0`, the tag resolves to upstream commit `dbfea35209dc...`, and the tree is clean.

**Step 2: Merge with history preserved**

Run: `git merge --no-ff v1.18.2`

Expected: Git creates a merge or stops with a finite conflict list.

**Step 3: Resolve conflicts by ownership**

Preserve StartOS-owned packaging and documentation. Take upstream application behavior and release files. Retain `@types/node` 22, workspace-package rewrites, `linkWorkspacePackages`, and StartOS dependency pins.

**Step 4: Verify ancestry**

Run: `git merge-base --is-ancestor 'v1.18.2^{}' HEAD && git show --no-patch --format='%H%n%P%n%s' HEAD`

Expected: exit 0 and a merge commit whose second parent is the upstream release commit.

### Task 2: Reproduce StartOS Adaptation Gaps

**Files:**
- Test: `startos/**/*.test.ts`
- Inspect: `packages/daemon/src/config/env-overrides.ts`
- Inspect: `Dockerfile.startos`
- Inspect: `package.json`
- Inspect: `pnpm-workspace.yaml`

**Step 1: Run the focused post-merge checks before corrections**

Run: `npm run check:lock-consistency && npm run typecheck`

Expected: any merge-created lock or type divergence fails for the specific missing adaptation; otherwise record a passing no-correction result.

**Step 2: Inspect configuration and image contracts**

Run: `rg -n "BHA_[A-Z0-9_]+" startos packages/daemon/src/config/env-overrides.ts && rg -n "^(COPY|FROM)|packages/.*/dist|javascript" Dockerfile.startos`

Expected: every injected variable remains accepted by upstream and every copied build output still exists.

**Step 3: Add a failing test only for an actual wrapper defect**

If an incompatibility exists, add the smallest real-code test under `startos/`, run that exact test, and confirm it fails for the expected missing behavior before changing production code.

**Step 4: Apply the minimal correction and rerun the focused test**

Expected: the focused test passes and no unrelated StartOS behavior changes.

### Task 3: Reconcile Dependency Metadata

**Files:**
- Modify: `package-lock.json`
- Modify: `pnpm-lock.yaml`
- Modify only if upstream changed constraints: `package.json`
- Modify only if upstream changed constraints: `pnpm-workspace.yaml`

**Step 1: Preserve fork-only declarations**

Confirm `@types/node` remains on major 22, internal workspace dependencies remain `*`, and `linkWorkspacePackages: true` remains present.

**Step 2: Regenerate npm's lockfile**

Run: `npm install --package-lock-only`

Expected: npm resolves the merged dependency graph without modifying source.

**Step 3: Reconcile pnpm only where the consistency check identifies drift**

Run the exact `pnpm update -r --lockfile-only <packages>` set derived from the failing consistency output.

**Step 4: Verify both lockfiles agree**

Run: `npm run check:lock-consistency`

Expected: PASS for all direct dependencies and importers.

### Task 4: Update StartOS Release Metadata

**Files:**
- Modify: `startos/utils.ts`
- Modify: `startos/versions/current.ts`
- Test: existing StartOS compilation and package inspection gates

**Step 1: Verify the old metadata is detectable**

Run: `rg -n "1\.18\.0" startos/utils.ts startos/versions/current.ts`

Expected: matches prove the candidate still advertises the old upstream version.

**Step 2: Update application and package versions**

Set `appVersion` to `1.18.2` and package ExVer to `1.18.2:0`.

**Step 3: Rewrite all localized release notes**

Summarize unpaid-history protection/recovery, failed-action explanations, account churn/blacklist/node-down safeguards, truthful chart/Timeline states, the upstream maintenance-mode notice, automatic app migration 0124, and link to `https://github.com/rdouma/hashrate-autopilot/releases/tag/v1.18.2`.

**Step 4: Verify TypeScript compilation**

Run: `npm run typecheck`

Expected: PASS across StartOS and all workspaces.

### Task 5: Synchronize Operator Documentation

**Files:**
- Modify only if behavior requires it: `README.md`
- Modify only if behavior requires it: `instructions.md`
- Modify through upstream merge: `CHANGELOG.md`
- Modify through upstream merge: `rdouma-hashrate-autopilot/umbrel-app.yml`
- Modify through upstream merge: `rdouma-hashrate-autopilot/docker-compose.yml`

**Step 1: Compare documentation to the merged behavior**

Confirm fixed README headings, actions, tasks, dependencies, volumes, health checks, backup behavior, DRY-RUN limitations, account-protection holds, node-down behavior, and BIP110 limitations.

**Step 2: Apply only StartOS-specific corrections**

Keep version strings out of README and instructions. Do not duplicate general upstream documentation.

**Step 3: Verify Umbrel synchronization**

Run: `rg -n "version:|image:" rdouma-hashrate-autopilot/umbrel-app.yml rdouma-hashrate-autopilot/docker-compose.yml`

Expected: both retain upstream's v1.18.2 release values and the image pin uses bare semver.

**Step 4: Format-check changed documentation and metadata**

Run: `npx prettier --check README.md instructions.md startos/utils.ts startos/versions/current.ts`

Expected: PASS.

### Task 6: Run Repository Gates

**Files:**
- No source edits unless a newly reproduced defect requires a red/green correction

**Step 1: Run whitespace validation**

Run: `git diff --check`

Expected: PASS.

**Step 2: Run the complete native gate**

Run: `npm run check`

Expected: lock consistency, lint, typecheck, and all tests pass; record warnings separately.

**Step 3: Run SDK lint**

Run: `node node_modules/@start9labs/start-sdk/lint.mjs`

Expected: PASS.

**Step 4: Commit the reviewed merge adaptations**

Review `git diff --stat`, `git diff`, and `git status --short`, then commit only the reviewed v1.18.2 sync work.

### Task 7: Build and Inspect Both Architectures

**Files:**
- Ignored artifact: `hashrate-autopilot_x86_64.s9pk`
- Ignored artifact: `hashrate-autopilot_aarch64.s9pk`
- Ignored evidence: `SHA256SUMS`

**Step 1: Confirm the exact clean source commit**

Run: `git status --porcelain && git rev-parse HEAD`

Expected: clean tracked tree and one reviewed commit SHA.

**Step 2: Build x86_64**

Run: `make x86`

Expected: PASS and one x86_64 package artifact.

**Step 3: Build aarch64**

Run: `make arm`

Expected: PASS and one aarch64 package artifact from the same commit.

**Step 4: Inspect both packages**

Use `start-cli s9pk inspect <artifact> manifest` and `commitment`. Confirm package ID `hashrate-autopilot`, version `1.18.2:0`, matching architecture, image, release notes, and source hash.

**Step 5: Record checksums**

Run: `sha256sum hashrate-autopilot_x86_64.s9pk hashrate-autopilot_aarch64.s9pk > SHA256SUMS && sha256sum -c SHA256SUMS`

Expected: both artifacts pass.

### Task 8: Close Local Evidence and Prepare Handoff

**Files:**
- Update: tracked StartOS evidence checklist for this candidate

**Step 1: Run the final Community audit**

Run: `python3 /home/missydog/.codex/skills/preparing-startos-community-submissions/scripts/audit_repository.py --root . --format text`

Expected: no deterministic FAIL; keep every WARN and MANUAL item unresolved unless explicit evidence closes it.

**Step 2: Inspect hosted Community evidence**

Record existing PR URLs, head commit, workflow matrix, and current checks without changing the Start9 repository.

**Step 3: Record device gaps**

Do not reuse v1.17.4 or v1.18.0 device evidence for v1.18.2. Preserve DRY-RUN and leave install, start, UI, health, restart, uninstall, reinstall, backup, and restore as MANUAL until run on an identity-verified device.

**Step 4: Prepare the handoff report**

Report exact commit, merge ancestry, local checks, artifacts, checksums, inspection results, warnings, manual gaps, and the safe next action for the existing Community PR. Do not push, tag, publish, open or modify PRs, or request promotion.
